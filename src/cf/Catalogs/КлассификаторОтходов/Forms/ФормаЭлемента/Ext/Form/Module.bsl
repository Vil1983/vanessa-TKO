
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если НЕ РольДоступна("Администратор") И Объект.Владелец = Справочники.Пользователи.НайтиПоНаименованию("Администратор") Тогда
		ЭтаФорма.Элементы.Наименование.Доступность 		 = Ложь;
		ЭтаФорма.Элементы.НаименованиеПолное.Доступность = Ложь; 
		ЭтаФорма.Элементы.Родитель.Доступность			 = Ложь;
	КонецЕсли;
	
КонецПроцедуры
